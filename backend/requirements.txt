Django>=5.2,<5.3
django-ninja>=1.4,<2
django-cors-headers>=4.4
psycopg[binary]>=3.2,<4
python-dotenv>=1.0
boto3>=1.34          # AWS S3 uploads for character images
requests>=2.31       # calling the Midjourney (AI image) proxy API

# MCP server (backend/mcp_server) — a stdio MCP server that fronts this API for
# AI game-creation agents. Not imported by Django; only needed to run `python -m mcp_server`.
mcp>=1.2,<2          # Model Context Protocol SDK. Pinned to v1: server.py uses FastMCP,
                     # which v2 renamed to MCPServer (migration is a separate change).
httpx>=0.27          # async HTTP client the MCP tools call the REST API with
